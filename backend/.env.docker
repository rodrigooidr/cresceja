# ===========================
# CresceJá - .env (DOCKER)
# Usado pelos containers backend/worker
# ===========================

# Postgres pelo NOME DO SERVIÇO do compose
DATABASE_URL=postgres://cresceja:cresceja123@postgres:5432/cresceja_db

# Redis pelo NOME DO SERVIÇO do compose
REDIS_URL=redis://redis:6379

CRESCEJA_ENC_KEY=,o4>(pbU"e5ma^:g9S]XEI78.Yi+_Qdz

# Porta exposta pelo container do backend
PORT=4000

# CORS (frontend no host)
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# JWT
JWT_SECRET=dev-change-me

# Ambiente / Logs
NODE_ENV=development
LOG_LEVEL=info

# Worker (BullMQ)
REPURPOSE_CONCURRENCY=2
REPURPOSE_ATTEMPTS=3
REPURPOSE_BACKOFF_MS=2000

# Modo de teste do WS
WS_TEST_MODE=true
ALLOWED_WS_TEST_EMAIL=rodrigooidr@hotmail.com

# Uploads (volume mapeado em /app/uploads)
UPLOAD_DIR=./uploads

# ===== Integrações (preencha se usar) =====
WHATSAPP_PROVIDER=
WHATSAPP_TOKEN=
WHATSAPP_PHONE_NUMBER_ID=
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_FROM=
WH_VERIFY_TOKEN=
SENTRY_DSN=
WA_PHONE_NUMBER_ID=
WA_TOKEN=
IG_VERIFY_TOKEN=dev
FB_VERIFY_TOKEN=dev

# Google OAuth
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:4000/api/calendar/google/callback

# Azure OAuth
AZURE_TENANT_ID=common
AZURE_CLIENT_ID=
AZURE_CLIENT_SECRET=
AZURE_REDIRECT_URI=http://localhost:4000/api/calendar/outlook/callback

# Taxas do produto (ex.: IA)
RATE_ATTEND=0.0005
RATE_CONTENT=0.02

# Telemetria & Handoff
TELEMETRY_ENABLED=true
HANDOFF_AUTO_DISABLE_IA=true
HANDOFF_ALERT_SOUND_URL=/assets/alerts/handoff.mp3
